--File generated using Love Game Maker (https://github.com/Radfordhound/love2d-game-maker)
{0} = class('{0}')

function {0}:draw()
	love.graphics.print("x = " .. self.x .. ", y = " .. self.y,self.x,self.y)
end

function {0}:step()
	{3}
end

function {0}:initialize(x,y)
	self.x = x
	self.y = y
	self.id = {1}
	self.tp = "{0}"

	{2}
end

return {0}