--File generated using Love Game Maker (https://github.com/Radfordhound/love2d-game-maker)
{0} = class('{0}')

function {0}:draw()
	for i = 1, table.getn(self.objects) do
		self.objects[i]:draw()
	end
end

function {0}:update()
	for i = 1, table.getn(self.objects) do
		self.objects[i]:step()
	end
end

function {0}:initialize(objects)
	self.tp = "{0}"
end

function {0}:create()
	self.objects = {{}}
	{1}
end

return {0}